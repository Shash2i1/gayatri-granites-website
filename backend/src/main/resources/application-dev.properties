server.port=8081

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/gayatri_granites_test_db
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

#AWS s3
#https://251128026602.signin.aws.amazon.com/console
#gayatri_granites_S3
# AWS S3
aws.s3.bucket=gayatri-granites-images
aws.s3.region=ap-southeast-2
aws.s3.access-key=${AWS_ACCESS_KEY}
aws.s3.secret-key=${AWS_SECRET_KEY}

app.admin.emails=attendance.hub.01@gmail.com, manager@yourgranitestore.com

# JWT Configuration
jwt.secret=${JWT_SECRET}

frontend.url=http://localhost:5173